md-toolbar(class="topBar", md-scroll-shrink)
    div(class="md-toolbar-tools")
        h3(flex)
            span Follow Ups
        md-button(class="md-icon-button", aria-label="Search")
            md-icon search
        md-button(class="md-icon-button", aria-label="Add", ng-click="actions.showNewFollowUp()")
            md-icon add

md-content(flex, layout="column")
    md-list(class="followUps", flex)
        md-list-item(class="md-3-line" ng-repeat="followup in data.followUps.followUps" ng-click="null")
            md-icon(class="md-avatar") person
            div(class="md-list-item-text" layout="column")
                h3 {{followup.name}}
                h4(ng-hide="followup.carecell") Tiada Carecell
                h4(ng-show="followup.carecell") Carecell: {{followup.carecell.name}}
                p
                    span Tanggal Lahir: {{followup.dob | date : 'd MMM yyyy'}},
                    span(ng-show="followup.gender == 'female'") Perempuan
                    span(ng-show="followup.gender == 'male'") Laki-laki